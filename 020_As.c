//program name: 020_As.c
#include<stdio.h>
int main()
{
  int m;
  float tkm;
  printf("total traviled by the bus from A to B in kilometers...:");
  scanf("%f",&tkm);
  m=tkm*1000;
  printf("total traviledby the bus from A to B in meters...:%d",m);
  return 0;
}
